# 复制为 .env 后填写
# DeepSeek API key，去 https://platform.deepseek.com 申请
DEEPSEEK_API_KEY=sk-xxxxxxxx

# 没填 key 或 MOCK=1 时，用内置示例数据跑通全流程
# 填了 key 想用真实引擎：删掉这行或设 MOCK=0
MOCK=1

# 可选：默认 deepseek-v4-flash（便宜快）；想要更强推理可换 deepseek-v4-pro
# DEEPSEEK_MODEL=deepseek-v4-flash

# 端口
PORT=8700
